<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <size
        android:width="@dimen/connection_status_size"
        android:height="@dimen/connection_status_size" />
    <stroke
        android:width="@dimen/connection_status_contour"
        android:color="@color/bordeaux" />
    <solid android:color="@color/bordeaux" />
</shape>